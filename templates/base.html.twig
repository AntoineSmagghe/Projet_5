<!DOCTYPE html>
<html lang="fr-FR">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>{% block title %}{% endblock %}</title>

        <!-- FAVICONS -->
        <link rel="apple-touch-icon" sizes="57x57" href="/img/favicons/apple-icon-57x57.png">
        <link rel="apple-touch-icon" sizes="60x60" href="/img/favicons/apple-icon-60x60.png">
        <link rel="apple-touch-icon" sizes="72x72" href="/img/favicons/apple-icon-72x72.png">
        <link rel="apple-touch-icon" sizes="76x76" href="/img/favicons/apple-icon-76x76.png">
        <link rel="apple-touch-icon" sizes="114x114" href="/img/favicons/apple-icon-114x114.png">
        <link rel="apple-touch-icon" sizes="120x120" href="/img/favicons/apple-icon-120x120.png">
        <link rel="apple-touch-icon" sizes="144x144" href="/img/favicons/apple-icon-144x144.png">
        <link rel="apple-touch-icon" sizes="152x152" href="/img/favicons/apple-icon-152x152.png">
        <link rel="apple-touch-icon" sizes="180x180" href="/img/favicons/apple-icon-180x180.png">
        <link rel="icon" type="image/png" sizes="192x192"  href="/img/favicons/android-icon-192x192.png">
        <link rel="icon" type="image/png" sizes="32x32" href="/img/favicons/favicon-32x32.png">
        <link rel="icon" type="image/png" sizes="96x96" href="/img/favicons/favicon-96x96.png">
        <link rel="icon" type="image/png" sizes="16x16" href="/img/favicons/favicon-16x16.png">

        <link href="https://fonts.googleapis.com/css?family=Noto+Serif|Open+Sans&display=swap" rel="stylesheet">
        <script src="https://kit.fontawesome.com/ef657a2258.js" crossorigin="anonymous"></script>
        <link rel="stylesheet" href="/css/base.css">
        <link rel="stylesheet" href="/css/keyframes.css">
        {% block header %}{% endblock %}
        {% block style %}{% endblock %}
    </head>
    <body>
        <header>
            <nav>
                <div id="background_modal"></div>
                <a href="/">
                    <img id="logo_header" src="/img/CDLM_logo.svg" alt="Logo CDLM">
                </a>
                <ul>
                    <li class="item_menu">
                        <a class="link_menu" href="{{ path("articles", {"format": "news"}) }}">News</a>
                    </li>
                    {% if app.user != null %}
                    <li class="item_menu">
                        <button class="btn_dropdown_menu">Events</button>
                        <div class="dropdown_menu">
                            <a href="{{ path("articles", {"format": "publicEvent"}) }}" class="link_dropdown_menu">Public</a>
                            <a href="{{ path("articles", {"format": "privateEvent"}) }}" class="link_dropdown_menu">Private</a>
                        </div>
                    </li>
                    {% else %}
                    <li class="item_menu">
                        <a href="{{ path("articles", {"format": "publicEvent"}) }}" class="link_menu">Events</a>
                    </li>
                    {% endif %}
                    <li class="item_menu">
                        <button class="btn_dropdown_menu">Label</button>
                        <div class="dropdown_menu">
                            <a class="link_dropdown_menu" href="{{ path("articles", {"format": "members"}) }}">Artistes</a>
                            <a class="link_dropdown_menu" href="{{ path("articles", {"format": "releases"}) }}">Releases</a>
                            <a class="link_dropdown_menu" href="#">About</a>
                        </div>
                    </li>
                    <li class="item_menu">
                        <a class="link_menu" href="#">About</a>
                    </li>
                    {% if app.user == null %}
                    <li class="item_menu">
                        <img class="sign_in_btn" alt="Logo de connexion" src="/img/user-circle-regular.svg">
                        <div class="sign_in_form">
                            <form action="{{ path('login') }}" method="post">
                                <div class="form_row">
                                    <label for="mail">Email</label>
                                    <input type="text" id='mail' name="_username" placeholder="Adresse mail" class="form-control">
                                </div>
                                <div class="form_row">
                                    <label for="password">Mot de passe</label>
                                    <input type="password" id="password" name="_password" placeholder="Mot de passe" class="form-control">
                                </div>
                                <div class="form_row">
                                    <input type="hidden" name="_token" value="{{ csrf_token('submitLogin') }}">
                                    <button type="submit" class=".a_btn a_btn btn_more">Envoyer</button>
                                </div>
                            </form>
                        </div>
                    </li>
                    {% else %}
                    <li class="item_menu">
                        <img src="/img/ellipsis-h-solid.svg" alt="Logo informations admin" class="sign_in_btn">
                        <div class="sign_in_form">
                            {% if is_granted("ROLE_ADMIN") %}
                            <a class="admin_menu" href="{{ path("creatPost") }}">Create post</a>
                            <a class="admin_menu" href="{{ path("signIn") }}">Create member</a>
                            {% endif %}
                            <a class="admin_menu" href="{{path("account")}}">Account</a>
                            <a class="admin_menu" href="{{ path("logout") }}">Logout</a>
                        </div>
                    </li>
                    {% endif %}
                </ul>                        
            </nav>
        </header>
        {# read and display all flash messages #}
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="flash-{{ label }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
        {% block body %}{% endblock %}
        <footer>
            <div class="footer_card">
                <h1>Copyright</h1>
                <p>Antoine Smagghe</p>
            </div>
            <div class="footer_card">
                <h1>Contact</h1>
            </div>
            <div class="footer_card">
                <h1>Legals</h1>
            </div>
            <div class="footer_card">
                <h1>Menu</h1>
            </div>
        </footer>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
        <script src="/js/menuHidden.js"></script>
        <script src="/js/Modal.js"></script>
        <script type="application/javascript">
            window.addEventListener("load", ()=>{
                //Sub menus dropdown when mouse over.
                let btn_dropdown_menu = document.getElementsByClassName("btn_dropdown_menu");
                let dropdown_menu = document.getElementsByClassName("dropdown_menu");
                for (let i = 0; i < btn_dropdown_menu.length; i++){
                    let burgerMenu = new menuHidden(
                        btn_dropdown_menu[i], 
                        dropdown_menu[i], 
                        "flex", 
                        "scaleBlur", 
                        "scaleBlur"
                    );
                    burgerMenu.listenIco();
                }

                //Form appear when click on icon.
                let signinModal = new Modal(
                    document.getElementsByClassName("sign_in_form")[0],
                    "flex",
                    document.getElementById("background_modal"),
                    document.getElementsByClassName("sign_in_btn")[0],
                );
                signinModal.init();
            });
        </script>
        {% block javascripts %}{% endblock %}
    </body>
</html>
